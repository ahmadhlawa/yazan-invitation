/**
 * توقيتات الحركة المشتركة بين جافاسكربت وCSS.
 *
 * تسلسل البوّابة مكتوب في invitation.css (تأخيرات transition/animation)،
 * لكن ثلاثة أطراف تحتاج المدّة نفسها: App لرفع قفل التمرير، وCoupleNames
 * لتبدأ لحظة الأسماء عند انقشاع البوّابة لا خلفها. تُعرَّف هنا مرّة واحدة.
 */

/** مدّة تسلسل فتح البوّابة كاملًا — يطابق أطول تأخير في .gate داخل invitation.css */
export const GATE_OPEN_MS = 2500;

/** مدّة مختصرة عند تفضيل تقليل الحركة */
export const GATE_OPEN_REDUCED_MS = 320;

/**
 * اللحظة التي تبدأ عندها الأسماء بالظهور (بالثواني).
 * أبكر قليلًا من انتهاء البوّابة حتى يلتقط المشاهد بداية الدعاء وهو يرى
 * المشهد ينكشف، فلا تقع فجوة ساكنة بين المشهدين.
 */
export const NAMES_LEAD_S = 2.1;
